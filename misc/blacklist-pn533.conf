# The following Linux kernel modules interfere with pcscd / libccid.
#
# If, for example, you have an ACR122U reader connected to USB and pcsc_scan
# doesn't seem to see the reader, copy this file in /etc/modprobe.d, then rmmod
# the modules manually or reboot the machine
blacklist pn533_usb
blacklist pn533
